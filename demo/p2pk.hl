
mod witness {
    const ALICE_PUBLIC_KEY: u256 = 0;
    const ALICE_SIGNATURE: [u8; 64] = 0;
}

fn main() {
    jet::bip_0340_verify(
        (witness::ALICE_PUBLIC_KEY, jet::sig_all_hash()),
        witness::ALICE_SIGNATURE
    )
}
